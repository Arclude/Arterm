import type { Theme } from "../types";

export const artexDefault: Theme = {
  id: "artex-default",
  name: "Artex Default",
  description: "The default Artex look — clean glass over neutral surfaces.",
  editorTheme: { dark: "atomone", light: "atomone" },
  variants: {
    light: {},
    dark: {},
  },
};
