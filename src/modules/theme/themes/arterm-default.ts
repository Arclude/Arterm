import type { Theme } from "../types";

export const artermDefault: Theme = {
  id: "arterm-default",
  name: "Arterm Default",
  description: "The default Arterm look — clean glass over neutral surfaces.",
  editorTheme: { dark: "atomone", light: "atomone" },
  variants: {
    light: {},
    dark: {},
  },
};
